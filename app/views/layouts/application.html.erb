<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Caitlin's Tax App! :)</title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Rails Bootstrap" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <main role="main">
      <div class="container">
        <% if current_user %>
        <div style='float:right;'><%= link_to "Settings", logout_path %> | <%= link_to "Logout", logout_path, :method => 'delete' %></div>
          <div class='text-center'><%= link_to image_tag('logo.png'), user_path(current_user) %></div>
        <% else %>
          <div class='text-center'><%= image_tag('logo.png') %></div>
        <% end %>
        <div style="width:100%;background-color:black;height:7px">&nbsp;</div>
        <br>
        <div class="content">
          <div class="row">
            <div class="span12">
              <%= render 'layouts/messages' %>
              <%= yield %>
            </div>
          </div>
          <footer>
          </footer>
        </div>
      </div>
    </main>
  </body>
</html>
